# KRISPIN — קליטה וקיטלוג של 441 טיוטות הוצאה ממורנינג

> מסמך הנחיות לסשן. קרא אותי קודם. כל ההקשר של המשימה נמצא כאן.

## המטרה
ללקוח (עוסק מורשה) יש **441 מסמכי הוצאה מספקים** שממתינים לאישור במורנינג (חשבונית ירוקה),
שהזיהוי האוטומטי שלהם לא הצליח במלואו. צריך:
1. **להוריד** את כל 441 המסמכים → Google Drive (תיקייה ייעודית).
2. **לסרוק כל מסמך לעומק** (קריאה אמיתית, לא OCR אוטומטי) ולחלץ את כל השדות.
3. לבנות **קובץ אקסל** מלא עם קידוד‑צבע של רמת הוודאות לכל נתון.
4. **לעצור — והלקוח עובר ומאשר את האקסל במפורש.**
5. רק לאחר אישור: **להטמיע** את הנתונים חזרה למורנינג דרך ה‑API (לעדכן/לאשר את הטיוטות).

## כלל זהב
**בשלב זה: הורדה + סריקה + אקסל בלבד. לא מוחקים כלום ולא קולטים/מטמיעים כלום במורנינג**
עד אישור מפורש ונפרד של הלקוח. ההטמעה (שלב 5) מגודרת מאחורי אישור.

## אישור משתמש
הלקוח אישר לעבוד אוטומטית ולא לעצור לשאלות באמצע — **לרכז את כל השאלות לסוף**. להתקדם.

---

## גישה ל‑Morning API
- Base: `https://api.greeninvoice.co.il/api/v1`
- אימות: `POST /account/token` עם `{"id": KEY, "secret": SECRET}` → מחזיר `{token, expires}`.
- ההזדהות נקראת ע"י `morning_api.py` ממשתני הסביבה **`MORNING_API_KEY`** ו‑**`MORNING_API_SECRET`**
  (מוגדרים ברמת ה‑Environment; לא בקוד, לא ב‑git).
- הטיוטות "ממתין לאישור" הן **expense drafts**. צריך לאמת את ה‑endpoint המדויק לחיפוש/הורדה/עדכון
  מול ה‑API החי (ראה `discover_morning.py`). מסמכי הוצאה מאושרים = expenses.
- חובה לשמור לכל מסמך את ה‑**draft_id** לצורך הקישור לאקסל ולעדכון בשלב 5.

## Google Drive
- חובה שחיבור ה‑Google Drive (MCP) יהיה מופעל בסשן.
- **תיקיית היעד ל‑441 (חדשה, נפרדת):**
  - שם: `KRISPIN - 441 מסמכים לאישור (2026-06)`
  - id: `15PhIp59cLThaX715AMtgincHebBCNbyC`
- **אסור לגעת** בתיקיית ההוצאות המאושרות: `הוצאות/2026/...` (folder id `1lvPMc8qbGeWMQuFFVHIO5Z72tbbxFMUb`).
- שמות קבצים: `NNNN_<draft_id>_<שם-ספק>.<סיומת>` כדי לשמור קישור חד‑חד‑ערכי שורה↔מסמך.

---

## עמודות האקסל (לפי דרישת הלקוח)
1. שם ספק
2. מספר עוסק / ח.פ.
3. סוג מסמך
4. מספר מסמך
5. תאריך המסמך
6. תקופת דיווח (**דו‑חודשי** — לזכור)
7. סכום הוצאה כולל מע"מ
8. סך הכל מע"מ
9. שולם באמצעות (אשראי / העברה; אם אשראי — 4 ספרות אחרונות אם מופיע)
10. מטבע
11. מספר הקצאה (מספר הקצאה של חשבונית ישראל / allocation number)
12. שם הקובץ
13. הערות (לפי הצורך)
14. סיווג תשומה (ל‑PCN874)

עמודות עזר להוסיף: **סכום לפני מע"מ**, **#index**, **draft_id**, **קישור לקובץ ב‑Drive**, **רמת ודאות**.

### קידוד צבעים (חובה) — 3 דרגות ודאות לכל תא נתון
- 🟩 ירוק = ודאות גבוהה (נקרא ברור מהמסמך)
- 🟨 צהוב = ודאות בינונית (הוסק/חלקית ברור)
- 🟥 אדום = ודאות נמוכה / לא נמצא — דורש בדיקת הלקוח

### שדות קריטיים (לדייק עליהם במיוחד)
**מחיר/סכום, מספר מסמך, תאריך מסמך, שם ספק.**

---

## כללי סיווג ועסק (חשוב!)
- **כשיש ספק לגיטימי — לבחור את הסיווג שמפחית ללקוח את המס.** הלקוח עובר אחרי הכל.
- **תחום העסק:** מכירת פרסומות במשחקי כדורגל.
  → **רכישות שנעשות דרך קבוצות כדורגל = מלאי (לא הוצאת פרסום).**
- מכשירים (למשל סלולרי): לשקול מתנה / רכוש קבוע — לפי מה שמיטיב עם הלקוח.
- **הרבה קבלות/חשבוניות מחו"ל ללא מע"מ** — לזהות ולא לחשב מע"מ תשומות עליהן.
- **PCN874:** לחקור לעומק את סוגי רשומות התשומות והסיווג הנכון (ציוד/רכוש קבוע/תשומות אחרות/חו"ל)
  ולסמן שורות שמצריכות וידוא של הלקוח. המטרה הסופית: לאפשר הפקת **קובץ PCN874 תקין** לדיווח.

---

## תוכנית הרצה (לסשן החדש)
1. `python3 discover_morning.py` — לאמת אימות + לאתר את endpoint הטיוטות + לאשר ספירה = 441.
2. הורדה: למשוך את כל הטיוטות, להוריד קובץ מקור לכל אחת, להעלות ל‑Drive, ולכתוב `index.csv`
   (index, draft_id, supplier, filename, drive_id, server fields). throttling + retries.
3. סריקה באצוות (~20–30) עם שמירת התקדמות ב‑`catalog.json` (ניתן להמשך), קריאה אמיתית של כל מסמך.
4. לבנות `catalog.xlsx` עם הצבעים. **לעצור ולמסור ללקוח לאישור.**
5. (רק אחרי אישור) `upload_back.py` — לעדכן/לאשר טיוטות לפי draft_id; דוח הצלחות/כשלים.

## קבצים בפרויקט
- `morning_api.py` — לקוח API (אימות ממשתני סביבה).
- `discover_morning.py` — אימות + גילוי endpoints + ספירת טיוטות.
- `project_config.json` — מזהי תיקיות Drive + תצורה.
- `.env` — סודות מקומיים (מוחרג מ‑git; בסשן ענן הסודות מגיעים ממשתני הסביבה).

## הערות תפעוליות
- הסביבה זמנית — מה שלא נשמר ל‑Drive/ל‑git יימחק. לכן הקבצים → Drive.
- לפני push: לוודא ש‑`.env` ותיקיות המסמכים מוחרגים (קיים ב‑`.gitignore`).
