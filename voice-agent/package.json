{
  "name": "yahav-voice-sales-agent",
  "version": "1.0.0",
  "description": "Hebrew voice sales agent for Bank Yahav — bridges Twilio Media Streams to the OpenAI Realtime API and emails a call summary at the end.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check twilioHandler.js && node --check openaiRealtime.js && node --check conversation.js && node --check summary.js && node --check gmail.js && node --check persona.js && node --check knowledgeBase.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^6.9.13",
    "ws": "^8.17.0"
  }
}
